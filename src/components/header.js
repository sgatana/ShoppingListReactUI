import React, { Component } from  'react';

export default class Header extends Component {
  render(){
      return (
        <header>
            <h1>Epic Shopping List</h1>
            <p>Shopping with  a difference</p>
        </header>
    );
}
}